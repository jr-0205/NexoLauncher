<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="clr-namespace:NexoLauncher.App">

    <app:ProfileIconConverter x:Key="Nexo.ProfileIconConverter"/>
    <app:ProfileBackgroundConverter x:Key="Nexo.ProfileBackgroundConverter"/>

    <DataTemplate x:Key="Nexo.ProfileCardTemplate">
        <Border Width="248" Height="184" CornerRadius="12" ClipToBounds="True"
                Background="{StaticResource Nexo.Surface}" BorderBrush="{StaticResource Nexo.Border}" BorderThickness="1">
            <Grid>
                <Border>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#203F86"/>
                            <GradientStop Color="#172131" Offset="0.55"/>
                            <GradientStop Color="#090D13" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <Image Source="{Binding Id, Converter={StaticResource Nexo.ProfileBackgroundConverter}}"
                       Stretch="UniformToFill" Opacity="0.78"/>
                <Border>
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#15090D13"/>
                            <GradientStop Color="#45090D13" Offset="0.48"/>
                            <GradientStop Color="#F2090D13" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                <Grid Margin="14">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="54" Height="54" CornerRadius="10" Background="#E6172131"
                                BorderBrush="{StaticResource Nexo.BorderStrong}" BorderThickness="1"
                                HorizontalAlignment="Left" ClipToBounds="True">
                            <Grid>
                                <Image Source="{StaticResource Nexo.BrandMark}" Stretch="Uniform" Margin="9"/>
                                <Image Source="{Binding Id, Converter={StaticResource Nexo.ProfileIconConverter}}" Stretch="UniformToFill"/>
                            </Grid>
                        </Border>
                        <Border Grid.Column="1" Background="#D0172131" CornerRadius="7" Padding="8,5" VerticalAlignment="Top">
                            <TextBlock Text="NEXO" Foreground="#8DB3FF" FontSize="8" FontWeight="Bold"/>
                        </Border>
                    </Grid>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource Nexo.Text}" FontSize="15"
                                   FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource Nexo.TextSecondary}" FontSize="10"
                                   Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                        <StackPanel Orientation="Horizontal" Margin="0,9,0,0">
                            <Ellipse Width="6" Height="6" Fill="{StaticResource Nexo.Success}" VerticalAlignment="Center"/>
                            <TextBlock Text="  LISTO" Foreground="#80D9B9" FontSize="8" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </DataTemplate>
</ResourceDictionary>
